# Tasks API Demo

Тестовое задание (To-Do List).

REST API на Laravel для управления задачами.  
Позволяет создавать, просматривать, обновлять и удалять задачи.

---

## 🚀 Особенности проекта

- Используется встроенный сервер Laravel (`php artisan serve`)
- Для демо API не используется Nginx — упрощённая конфигурация
- Все миграции включены в проект
- Поле `status` по умолчанию = `false`
- API возвращает данные через **Resource**
- Валидация реализована через **FormRequest**

---

## 🛠 Стек технологий

- PHP 8.4 (Laravel)
- MySQL 8
- Docker + Docker Compose

---

## 📦 Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone git@github.com:Crocodilldo/TestTask.git
cd TestTask

2. Создать .env
cp .env.example .env

3. Собрать и запустить контейнеры
docker compose up - d --build

4. Выполнить миграции
docker compose exec app php artisan migrate:fresh

5. Приложение доступно по адресу:
http://localhost:8000
API Endpoints

    | Method | URL             | Description           |
    | ------ | --------------- | --------------------- |
    | GET    | /api/tasks      | Список всех задач     |
    | GET    | /api/tasks/{id} | Просмотр одной задачи |
    | POST   | /api/tasks      | Создание новой задачи |
    | PUT    | /api/tasks/{id} | Обновление задачи     |
    | DELETE | /api/tasks/{id} | Удаление задачи       |

🧪 Пример запроса
POST /api/tasks
{
  "title": "Новая задача",
  "text": "Описание задачи"
}
✅ Пример успешного ответа
GET /api/tasks/1
{
  "id": 1,
  "title": "Новая задача",
  "text": "Описание задачи",
  "status": false,
  "created_at": "2026-02-27T05:20:04.000000Z",
  "updated_at": "2026-02-27T05:20:04.000000Z"
}
📌 Примечание

Проект предназначен для демонстрации REST API в рамках тестового задания.
Для production рекомендуется использовать Nginx + php-fpm.
Так как это тестовое задание и методы неочень сложные, реализация логики была помещена в контроллер. Для более сложных реализаций логика выносится в сервисный слой.